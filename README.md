# Bloopy Network

A proactive Telegram-native creature game with managed personal bots, bounded bot-to-bot conversations, a populated story world, deterministic gameplay and optional BYOK narration.

## Development model

`main` stays deployable. Each feature is built on an `agent/*` branch and merged as one working vertical slice. The first product branch adds the complete playable foundation.
