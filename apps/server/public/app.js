const tg=window.Telegram?.WebApp;tg?.ready();tg?.expand();const headers={"content-type":"application/json"};if(tg?.initData)headers["x-telegram-init-data"]=tg.initData;let state;const $=(id)=>document.getElementById(id);function toast(message){$("toast").textContent=message;$("toast").classList.add("show");setTimeout(()=>$("toast").classList.remove("show"),2200)}async function api(path,options={}){const response=await fetch(path,{...options,headers:{...headers,...(options.headers||{})}});if(!response.ok)throw new Error((await response.json().catch(()=>({}))).error||"Request failed");return response.json()}function escapeHtml(value=""){return String(value).replace(/[&<>'"]/g,(char)=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[char]))}function storyCard(story){const choices=Array.isArray(story.choices)?story.choices:[];return `<article class="story"><h3>${escapeHtml(story.title)}</h3><p>${escapeHtml(story.body)}</p>${choices.length?`<div class="choices">${choices.map((choice)=>`<button data-action="${escapeHtml(choice.action)}">${escapeHtml(choice.label)}</button>`).join("")}</div>`:""}</article>`}function render(){const c=state.creature;$("creature-name").textContent=c.name;$("mood-line").textContent=`${c.mood} · ${c.current_location.replaceAll("_"," ")}`;$("avatar").src=`/api/creatures/${c.id}/avatar.svg?v=${c.level}`;$("energy").textContent=c.energy;$("level").textContent=`Lv ${c.level}`;$("xp").textContent=c.xp;$("story-feed").innerHTML=state.stories.map(storyCard).join("");$("npc-list").innerHTML=state.npcs.map((npc)=>`<div class="npc"><img src="/api/creatures/${npc.id}/avatar.svg"><b>${escapeHtml(npc.name)}</b><small>${escapeHtml(npc.current_location.replaceAll("_"," "))}</small></div>`).join("")}async function refresh(){state=await api("/api/bootstrap");render()}async function act(action){document.querySelectorAll("button[data-action]").forEach((b)=>b.disabled=true);try{const result=await api("/api/actions",{method:"POST",body:JSON.stringify({action})});toast(result.story.title);await refresh()}catch(error){toast(error.message)}finally{document.querySelectorAll("button[data-action]").forEach((b)=>b.disabled=false)}}document.addEventListener("click",(event)=>{const button=event.target.closest("button[data-action]");if(button)void act(button.dataset.action)});$("share").addEventListener("click",()=>{const latest=state.stories[0];const url=`${location.origin}/?startapp=meet_${state.creature.slug}`;const share=`https://t.me/share/url?url=${encodeURIComponent(url)}&text=${encodeURIComponent(`${state.creature.name}: ${latest.title}`)}`;tg?.openTelegramLink?tg.openTelegramLink(share):window.open(share,"_blank","noopener")});$("spawn-bot").addEventListener("click",async()=>{try{const{url}=await api("/api/bots/spawn-link");tg?.openTelegramLink?tg.openTelegramLink(url):window.open(url,"_blank","noopener")}catch(error){toast(error.message)}});$("ai-form").addEventListener("submit",async(event)=>{event.preventDefault();const values=Object.fromEntries(new FormData(event.currentTarget));try{await api("/api/settings/ai",{method:"POST",body:JSON.stringify(values)});event.currentTarget.reset();toast("Private model connection saved")}catch(error){toast(error.message)}});refresh().catch((error)=>toast(error.message));
